"""Начальное наполнение долговременной памяти — содержимое проекта миграции ГИС.

Это не демонстрационные данные «для галочки»: здесь лежит то, что реально
известно о мигрируемой системе и о принятых по ней решениях. Профиль задаёт
целевой стек и инварианты, знания описывают legacy-систему, журнал решений
начинается с выбора стека.

Наполнение вынесено в отдельный модуль, а не зашито в агента, ровно по той же
причине, по которой промпты не содержат фактов о проекте: память должна быть
данными, которые можно прочитать, поправить и подменить — иначе аблация слоёв
ничего не покажет, ведь всё важное будет сидеть в коде.

Публичное API:
  seed(memory, force=False)   — наполнить пустую долговременную память
  PROFILE / INVARIANTS / KNOWLEDGE / DECISIONS — сами данные
"""

from __future__ import annotations

from typing import Any

from agent import interview, preferences
from agent.memory.manager import MemoryManager
from agent.scenarios import Scenario, Step

# Рамки проекта: они одинаковы для всех пользователей и от личных предпочтений
# не зависят. Сами предпочтения сюда не входят — их пользователь задаёт сам
# (agent/interview.py).
PROFILE: dict[str, Any] = {
    "контекст": (
        "Мигрируем действующую геоинформационную систему газораспределительных "
        "сетей с PHP5/CodeIgniter/MapServer на Python/Django. Система в проде, "
        "ей пользуются диспетчеры, внешние потребители ходят по WMS/WFS, а часть "
        "данных приходит из 1С — большого «стоп-мира» на переписывание не будет."
    ),
    "ограничения": {
        "backend": "Python 3.12, Django 5.x + GeoDjango (django.contrib.gis)",
        "БД": "PostgreSQL 17 + PostGIS 3.6",
        "тайлы и OGC": "Martin для векторных тайлов, GeoServer для WMS/WFS-T",
        "frontend": "OpenLayers 10.x — нужен для редактирования геометрии, WFS-T и снэппинга",
        "деплой": "Docker Compose, внутри контейнеров Debian 13 (Trixie)",
        "legacy-стек": (
            "откуда мигрируем: Debian Jessie, Apache2 + PHP5, CodeIgniter 1, "
            "RedBean ORM, OpenLayers 2.13, MapServer 6.4, PostgreSQL 9.4 + PostGIS 2.1"
        ),
    },
}

# Инварианты. Жёсткие сформулированы так, чтобы их можно было проверить кодом:
# у каждого есть список слов или выражений, по которым идёт поиск в ответе.
INVARIANTS: list[dict[str, Any]] = [
    {
        "код": "стек-бэкенд",
        "правило": "Backend — только Django + GeoDjango; посторонние фреймворки не предлагать.",
        "тип": "запрет-слов",
        "значения": ["laravel", "symfony", "fastapi", "express", "nestjs", "yii", "flask"],
    },
    {
        "код": "legacy-не-развиваем",
        "правило": (
            "Компоненты старой системы можно упоминать для соответствия, но писать "
            "на них новый код нельзя."
        ),
        # Ищутся не названия, а вызовы API старого стека. Название в коде
        # встречается законно — в комментарии «аналог userpgplace.php из
        # CodeIgniter»; а вот `$this->load->model(...)` в ответе означает, что
        # агент пишет новый код на CodeIgniter. Первая версия инварианта
        # запрещала сами названия и ловила на этом ложные срабатывания.
        "тип": "запрет-в-коде",
        "значения": [
            "<?php", "$this->load->", "$this->db->", "R::dispense", "R::store",
            "CI_Controller", "mysql_connect",
        ],
    },
    {
        "код": "бд",
        "правило": "База данных — только PostgreSQL с PostGIS; решений под другие СУБД не предлагать.",
        "тип": "запрет-слов",
        "значения": ["mysql", "mariadb", "mongodb", "sqlite", "oracle", "mssql"],
    },
    {
        "код": "секреты-в-url",
        "правило": (
            "Секреты и одноразовые пароли не передаются в URL — только токен-based "
            "авторизация через заголовок (JWT/OAuth2)."
        ),
        "тип": "запрет-регулярок",
        "значения": [r"[?&](password|passwd|pwd|token|secret|api_?key)=", r"://[^/\s]+:[^/@\s]+@"],
    },
    {
        "код": "1С-источник-истины",
        "правило": (
            "1С остаётся источником истины для бизнес-данных объектов сети: писать "
            "бизнес-объекты напрямую в gisdata, минуя 1С, нельзя."
        ),
        "тип": "мягкий",
    },
    {
        "код": "внешняя-совместимость",
        "правило": (
            "Внешняя совместимость по WMS/WFS должна сохраняться: этими протоколами "
            "пользуются внешние потребители и, вероятно, сама 1С."
        ),
        "тип": "мягкий",
    },
]

# Знания о legacy-системе. Теги — это то, чем PromptBuilder отбирает факты под
# стадию: на planning нужна структура схем, на execution — конкретный компонент.
KNOWLEDGE: list[dict[str, Any]] = [
    {
        "id": "схема-gissys",
        "тема": "структура БД",
        "текст": (
            "Схема gissys хранит настройки и права: account (пользователи), group "
            "(группы), таблицы ms* (настройки слоёв MapServer), таблицы ol* "
            "(настройки OpenLayers), organization (разграничение видимости данных "
            "между организациями)."
        ),
        "теги": ["planning", "бд", "gissys", "схема", "права", "миграция"],
    },
    {
        "id": "схема-gisdata",
        "тема": "структура БД",
        "текст": (
            "Схема gisdata хранит сами геообъекты и их координаты. Наполняется "
            "преимущественно синхронизацией из 1С через внешний источник данных "
            "«ГеоИнформационнаяСистема»."
        ),
        "теги": ["planning", "бд", "gisdata", "схема", "геометрия", "1с", "миграция"],
    },
    {
        "id": "контроллеры-codeigniter",
        "тема": "прикладной код",
        "текст": (
            "Ключевые контроллеры CodeIgniter: userpgplace.php (методы listform, edit, "
            "savegeom, savenewcrash, starteditsession), openlayers.php (getConfig, search, "
            "layerswitcher), auth.php (вход и сессии), mapserver.php (генерация mapfile)."
        ),
        "теги": ["planning", "execution", "контроллеры", "php", "codeigniter", "api"],
    },
    {
        "id": "интеграция-1с",
        "тема": "интеграции",
        "текст": (
            "1С вызывает ГИС методами ЗаписатьОбъектВГИС, УдалитьОбъектИзГИС и "
            "ПолнаяСинхронизация; в конфигураторе подписки на события идут с префиксом "
            "«огГИС». Для мобильных клиентов есть отдельный веб-сервис mobile."
        ),
        "теги": ["planning", "execution", "1с", "интеграция", "синхронизация", "api"],
    },
    {
        "id": "доменная-логика-сети",
        "тема": "доменная логика",
        "текст": (
            "Газопроводы привязываются к точечным объектам; оборудование привязывается "
            "к газопроводам с расчётом угла поворота значка. Эмуляция отключений ищет "
            "родителя объекта и ближайшую арматуру, способную отсечь часть сети."
        ),
        "теги": ["planning", "execution", "домен", "сеть", "отключения", "газопровод"],
    },
    {
        "id": "две-версии-интерфейса",
        "тема": "фронтенд",
        "текст": (
            "У интерфейса две версии: полная — каждый слой MapServer отдельным слоем "
            "OpenLayers, и light — слои объединены в один «Объекты ГРС» и убран слой "
            "Google. Light сделан ради скорости перерисовки на слабых машинах."
        ),
        "теги": ["planning", "execution", "фронт", "openlayers", "слои", "производительность"],
    },
    {
        "id": "legacy-версии",
        "тема": "инфраструктура",
        "текст": (
            "Действующий прод: Debian Jessie, Apache2 + PHP5, CodeIgniter 1, RedBean ORM, "
            "MapServer 6.4, OpenLayers 2.13, PostgreSQL 9.4 + PostGIS 2.1. Все компоненты "
            "давно вне поддержки, обновление по частям невозможно."
        ),
        "теги": ["planning", "инфраструктура", "версии", "legacy", "деплой"],
    },
]

DECISIONS: list[dict[str, Any]] = [
    {
        "title": "Целевой стек — Python/Django/GeoDjango",
        "decision": (
            "Backend переписывается на Python 3.12 + Django 5.x с GeoDjango, база — "
            "PostgreSQL 17 + PostGIS 3.6, тайлы отдаёт Martin, WMS/WFS-T — GeoServer, "
            "фронтенд — OpenLayers 10.x."
        ),
        "reason": (
            "Нативная поддержка геометрии на уровне ORM, встроенная admin-панель под "
            "справочники и самый зрелый геостек из рассмотренных."
        ),
        "alternatives": [
            "Laravel — отклонён: геометрия только через сырой SQL",
            "FastAPI — отклонён: нет ORM с геометрией и админки, всё пришлось бы писать руками",
        ],
    },
    {
        "title": "Модели по точкам вызова выбраны по замеру, а не по ярлыку",
        "decision": (
            "Классификацию реплик ведёт glm-4.5-flash, свёртку задач — gpt-oss-20b, "
            "планирование и основные ответы — gpt-oss-120b, платный deepseek-v4-pro "
            "подключается только эскалацией после двух неудачных попыток."
        ),
        "reason": (
            "Прогон одинаковых заданий проекта на всех доступных моделях показал, что "
            "ярлык «слабая/сильная» решения не заменяет: allam-2-7b отнёс факт о числе "
            "таблиц к профилю вместо знаний и выдал GeoDjango-модель с декоратором "
            "python_2_unicode_compatible, убранным из Django ещё в 3.0. При этом на "
            "коде для PostGIS платные модели не дали заметного преимущества перед "
            "бесплатной gpt-oss-120b — платить за них на этой задаче незачем."
        ),
        "alternatives": [
            "allam-2-7b первой моделью маршрутизации — отклонён: ошибся на пробе классификации",
            "deepseek-v4-pro по умолчанию на исполнении — отклонён: то же качество кода при ненулевой цене",
        ],
    },
]


# Сценарии: во что превращается запрос пользователя. Это не «промпт с
# перечислением ролей» — каждый шаг идёт отдельным вызовом своей модели и видит
# только то, что ему назвали во «входе».
#
# Первому шагу каждого сценария разрешено остановить работу и спросить
# (может_спросить=True): он собирает требования, и без ответа идти дальше ему
# нечем. Остальным шагам это не даётся намеренно — backend, получивший проект от
# архитектора, должен писать код, а не переспрашивать.
SCENARIOS: list[Scenario] = [
    Scenario(
        имя="напиши фичу",
        описание="Полный цикл: требования, проект, серверный код, карта, ревью.",
        триггеры=["напиши фичу", "сделай фичу", "новая фича", "реализуй фичу"],
        шаги=[
            Step(
                агент="аналитик", роль="планирование", стадия="planning",
                вход=["запрос"], может_спросить=True,
                описание="Ты — системный аналитик, знающий старую ГИС газовых сетей.",
                задача=(
                    "Выпиши требования к фиче: что именно должно получиться, каких "
                    "объектов сети касается, что на это влияет в старой системе и "
                    "каких сведений не хватает. Без проектных решений и без кода."
                ),
            ),
            Step(
                агент="архитектор", роль="планирование", стадия="planning",
                вход=["запрос", "аналитик"],
                описание="Ты — архитектор новой системы на Django/GeoDjango.",
                задача=(
                    "По требованиям аналитика спроектируй решение: какие модели, "
                    "какие поля и геометрия, какие эндпоинты, что отдаётся тайлами, "
                    "а что через WFS. Укажи соответствие legacy-компонентам. Код не пиши."
                ),
            ),
            Step(
                агент="backend", роль="исполнение", стадия="execution",
                вход=["архитектор"],
                описание="Ты — backend-разработчик на Python 3.12 и Django 5 с GeoDjango.",
                задача=(
                    "Напиши серверную часть по проекту архитектора: модели, миграции, "
                    "представления. Код целиком и рабочим, с импортами."
                ),
            ),
            Step(
                агент="фронтенд", роль="фронтенд", стадия="execution",
                вход=["архитектор"],
                описание="Ты — фронтенд-разработчик, делаешь карту на OpenLayers 10.",
                задача=(
                    "Напиши клиентскую часть: слой на карте, обработчики, "
                    "взаимодействие с эндпоинтами из проекта архитектора."
                ),
            ),
            Step(
                агент="ревьюер", роль="исполнение", стадия="validation",
                вход=["архитектор", "backend", "фронтенд"],
                описание="Ты — ревьюер, который ищет расхождения, а не хвалит.",
                задача=(
                    "Сверь написанное с проектом и с ограничениями проекта. Перечисли "
                    "конкретные расхождения и чем они грозят. Если расхождений нет — скажи прямо."
                ),
            ),
        ],
    ),
    Scenario(
        имя="разбери legacy",
        описание="Как устроен компонент старой системы и чем его заменить.",
        триггеры=["разбери legacy", "как устроен", "разбери компонент", "что делает"],
        шаги=[
            Step(
                агент="археолог", роль="планирование", стадия="planning",
                вход=["запрос"], может_спросить=True,
                описание="Ты разбираешь старую систему на PHP5/CodeIgniter/MapServer.",
                задача=(
                    "Опиши, как названный компонент работает сейчас: какие таблицы и "
                    "контроллеры задействованы, кто его вызывает, что сломается без него. "
                    "Опирайся только на известные факты, недостающее назови недостающим."
                ),
            ),
            Step(
                агент="проектировщик", роль="планирование", стадия="planning",
                вход=["археолог"],
                описание="Ты переводишь legacy-компоненты в термины Django/GeoDjango.",
                задача=(
                    "Предложи соответствие: чем этот компонент становится в новой системе, "
                    "что переносится один в один, а что придётся переделать по существу."
                ),
            ),
            Step(
                агент="рисковик", роль="исполнение", стадия="execution",
                вход=["археолог", "проектировщик"],
                описание="Ты отвечаешь за то, чтобы перенос не уронил прод.",
                задача=(
                    "Перечисли риски переноса и порядок работ, при котором система "
                    "остаётся рабочей на каждом шаге. Отдельно — что проверить до переключения."
                ),
            ),
        ],
    ),
    Scenario(
        имя="оцени задачу",
        описание="Декомпозиция и трудоёмкость без кода — для планирования работ.",
        триггеры=["оцени задачу", "сколько займёт", "оцени трудоёмкость", "во что обойдётся"],
        шаги=[
            Step(
                агент="декомпозиция", роль="планирование", стадия="planning",
                вход=["запрос"], может_спросить=True,
                описание="Ты раскладываешь работу на части, которые можно поручить людям.",
                задача=(
                    "Разложи задачу на работы, каждую — с понятным результатом. "
                    "Отметь, какие из них зависят друг от друга."
                ),
            ),
            Step(
                агент="оценщик", роль="исполнение", стадия="execution",
                вход=["декомпозиция"],
                описание="Ты оцениваешь трудоёмкость и говоришь о рисках прямо.",
                задача=(
                    "Оцени каждую работу в днях, назови самые неопределённые места и "
                    "скажи, чем можно снизить неопределённость. Код не пиши."
                ),
            ),
        ],
    ),
]


def seed(memory: MemoryManager, force: bool = False) -> dict[str, int]:
    """Наполняет долговременную память проектными данными.

    По умолчанию работает только на пустой памяти: перезаписать профиль,
    который пользователь успел поправить под себя, — худшее, что может сделать
    инициализация. force=True наполняет поверх.
    """
    сводка = memory.long.stats()
    уже_есть = сводка["решения"] or сводка["знания"] or сводка["инварианты"]
    if уже_есть and not force:
        return {"профиль": 0, "инварианты": 0, "знания": 0, "решения": 0, "сценарии": 0}

    # Предпочтения — личное дело пользователя, и наполнять их за него нельзя:
    # тогда мастер настройки не сработает ни разу, а человек получит чужой
    # стиль ответов. Исключение одно: если пользователя зовут так же, как
    # заготовку («--кто тимлид»), он явно просит именно её.
    профиль = memory.long.profile.load()
    if memory.user_id in interview.TEMPLATES:
        профиль = interview.from_template(memory.user_id, профиль)
    профиль["user_id"] = memory.user_id
    # Ограничения и контекст описывают проект, а не человека, поэтому достаются
    # всем. Контекст заготовки, если он был, здесь и перекрывается: заготовка
    # описывает человека, а это — систему, которую переписывают.
    профиль["контекст"] = PROFILE["контекст"]
    профиль["ограничения"] = dict(PROFILE["ограничения"])
    memory.long.profile.save(профиль)

    for инвариант in INVARIANTS:
        memory.long.profile.add_invariant(dict(инвариант))

    for факт in KNOWLEDGE:
        memory.long.knowledge.add(
            факт["id"], факт["текст"], topic=факт["тема"], tags=факт["теги"],
            source="документ с описанием legacy-системы",
        )

    существующие = {з["заголовок"] for з in memory.long.decisions.all()}
    добавлено = 0
    for решение in DECISIONS:
        if решение["title"] in существующие:
            continue
        memory.long.decisions.add(
            решение["title"], решение["decision"], решение["reason"],
            решение["alternatives"], source="зафиксировано на старте проекта",
        )
        добавлено += 1

    сценариев = 0
    существующие_сценарии = {с.имя.lower() for с in memory.long.scenarios.all()}
    for сценарий in SCENARIOS:
        if сценарий.имя.lower() in существующие_сценарии:
            continue
        memory.long.scenarios.add(сценарий)
        сценариев += 1

    return {
        "профиль": len(preferences.FIELDS) + len(PROFILE["ограничения"]) + 1,
        "инварианты": len(INVARIANTS),
        "знания": len(KNOWLEDGE),
        "решения": добавлено,
        "сценарии": сценариев,
    }
