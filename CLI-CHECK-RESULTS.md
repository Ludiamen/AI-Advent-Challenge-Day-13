# Результат консольной проверки Дня 13

Прогон: `python check_cli.py`, модель **ds-flash**, память в `cli-check-memory/`.
Дата: 2026-09-20 02:41. Заняло 1.2 мин.

**Ожиданий проверено: 55, сошлось: 55, расхождений: 0.**

Каждая строка — отдельный запуск `cli.py` своим процессом. Это и есть
доказательство дня: состояние задачи живёт на диске, поэтому
продолжение работает в процессе, который о прошлом не знает ничего.

## Сводка

| шаг | проверка | команда | итог | секунд |
|---|---|---|---|---|
| 1.1 | Завести задачу | `--новая-задача перенос-организаций --название перенос справочника организаций` | ок | 0.9 |
| 1.2 | Три части состояния | `--задача перенос-организаций --состояние` | ок | 0.4 |
| 1.3 | Прыжок через этап отклоняется | `--задача перенос-организаций --завершить` | ок | 0.6 |
| 1.4 | План превращается в шаги (живой вызов) | `--задача перенос-организаций --план` | ок | 23.5 |
| 1.4б | Состояние после плана | `--задача перенос-организаций --состояние` | ок | 0.5 |
| 1.5 | Разрешённый переход выполняется | `--задача перенос-организаций --стадия execution` | ок | 0.3 |
| 2.1 | Пауза по команде | `--задача перенос-организаций --пауза` | ок | 0.4 |
| 2.2 | Пауза видна другому процессу | `--задача перенос-организаций --состояние` | ок | 0.3 |
| 2.3 | Ручную задачу продолжать нечем | `--задача перенос-организаций --продолжить` | ок | 0.3 |
| 3.1 | Сценарий по шагам встаёт на остановке | `--по-шагам --сценарий напиши фичу Напиши фичу: слой колодцев на карте` | ок | 5.8 |
| 3.2 | Задача сценария видна в списке | `--задачи` | ок | 0.3 |
| 4.1 | Продолжение в новом процессе (с ответом на вопрос шага) | `--по-шагам --задача напиши-фичу-0920-0240 --ответ Колодцы лежат в gisdata отдельным слоем, SRID 3857.` | ок | 9.8 |
| 4.2 | Продолжение в новом процессе | `--по-шагам --задача напиши-фичу-0920-0240 --продолжить` | ок | 15.8 |
| 4.3 | Продолжение в новом процессе | `--по-шагам --задача напиши-фичу-0920-0240 --продолжить` | ок | 13.8 |
| 5.1 | Пути слоёв памяти | `--файлы` | ок | 0.3 |
| 6.1 | Диалоговый режим | `--задача перенос-организаций` | ок | 0.4 |

## Подробности

### 1.1. Завести задачу

```
$ python cli.py --память-в cli-check-memory --модель ds-flash --новая-задача перенос-организаций --название перенос справочника организаций
```

- ✓ сказано, что задача заведена
- ✓ названа стадия planning
- ✓ код возврата 0

<details><summary>вывод</summary>

````
Задача «перенос-организаций» заведена, стадия planning.
````

</details>

### 1.2. Три части состояния

```
$ python cli.py --память-в cli-check-memory --модель ds-flash --задача перенос-организаций --состояние
```

- ✓ этап planning
- ✓ шагов нет — задача ведётся вручную
- ✓ ожидание «запустить»
- ✓ сказано, куда можно дальше
- ✓ код возврата 0

<details><summary>вывод</summary>

````
Задача «перенос-организаций», стадия planning (планирование).
------------------------------------------------------------------------------
ЗАДАЧА «перенос-организаций» — перенос справочника организаций
------------------------------------------------------------------------------
  этап       planning (планирование), идёт
             дальше можно: execution
  шаг        шагов нет — задача ведётся вручную
  ожидание   запустить — работа ещё не начиналась
------------------------------------------------------------------------------
````

</details>

### 1.3. Прыжок через этап отклоняется

```
$ python cli.py --память-в cli-check-memory --модель ds-flash --задача перенос-организаций --завершить
```

- ✓ отказ с названием стадии done
- ✓ названо, что разрешено
- ✓ код возврата 1

<details><summary>вывод</summary>

````
Задача «перенос-организаций», стадия planning (планирование).
Ошибка: Из стадии «planning» нельзя сразу в done. Разрешено: execution.
````

</details>

### 1.4. План превращается в шаги (живой вызов)

```
$ python cli.py --память-в cli-check-memory --модель ds-flash --задача перенос-организаций --план
```

- ✓ план напечатан
- ✓ шаги записаны в рабочую память
- ✓ код возврата 0

<details><summary>вывод</summary>

````
Задача «перенос-организаций», стадия planning (планирование).

1. Инвентаризация legacy-таблицы organization в схеме gissys: выгрузить DDL, список колонок, типы, ограничения, индексы, внешние ключи и объём строк из PostgreSQL 9.4.
2. Собрать фактические данные о связях organization с остальными таблицами gissys (account, group, ms*, ol*) и с бизнес-таблицами gisdata, чтобы понять, где организация используется как фильтр видимости.
3. Уточнить у владельца системы, кто и как наполняет справочник организаций: вручную через интерфейс, из 1С или обоими путями, и есть ли у организации атрибуты, приходящие из 1С.
4. Спроектировать целевую Django-модель Organization в приложении, отвечающем за настройки и права (аналог схемы gissys), с явным указанием полей, ограничений и связей с пользователями и группами.
5. Определить стратегию переноса данных: разовая миграция существующих записей organization из legacy-БД в PostgreSQL 17 + PostGIS 3.6 с сохранением идентификаторов, чтобы не порвать ссылки в account, group, ms*, ol*.
6. Описать правила разграничения видимости данных по организации в новом стеке (фильтрация queryset в Django/GeoDjango и, при необходимости, ограничения на стороне GeoServer для WMS/WFS), сохранив текущее поведение.
7. Зафиксировать проверки корректности переноса: сверка количества и контрольных сумм записей, проверка ссылочной целостности, проверка видимости данных для тестовых учётных записей разных организаций.

Не хватает сведений:
- фактической структуры таблицы organization (колонки, типы, ключи) — в знаниях о системе её нет;
- полного перечня таблиц, ссылающихся на organization, и характера этих ссылок;
- того, является ли 1С источником истины для самих организаций или только для объектов сети;
````

</details>

### 1.4б. Состояние после плана

```
$ python cli.py --память-в cli-check-memory --модель ds-flash --задача перенос-организаций --состояние
```

- ✓ шаг «1 из N, план»
- ✓ ожидание всё ещё «запустить»
- ✓ перечислены шаги
- ✓ код возврата 0

<details><summary>вывод</summary>

````
Задача «перенос-организаций», стадия planning (планирование).
------------------------------------------------------------------------------
ЗАДАЧА «перенос-организаций» — перенос справочника организаций
------------------------------------------------------------------------------
  этап       planning (планирование), идёт
             дальше можно: execution
  шаг        Инвентаризация legacy-таблицы organization в схеме gissys: выгрузить DDL, список колонок, типы, ограничения, индексы, внешние ключи и объём строк из PostgreSQL 9.4. (1 из 12, план)
  ожидание   запустить — работа ещё не начиналась

  шаги:
     1. Инвентаризация legacy-таблицы organization в схеме gissys: выгрузить DDL, список колонок, типы, ограничения, индексы, внешние ключи и объём строк из PostgreSQL 9.4. execution   ждёт
     2. Собрать фактические данные о связях organization с остальными таблицами gissys (account, group, ms*, ol*) и с бизнес-таблицами gisdata, чтобы понять, где организация используется как фильтр видимости. execution   ждёт
     3. Уточнить у владельца системы, кто и как наполняет справочник организаций: вручную через интерфейс, из 1С или обоими путями, и есть ли у организации атрибуты, приходящие из 1С. execution   ждёт
     4. Спроектировать целевую Django-модель Organization в приложении, отвечающем за настройки и права (аналог схемы gissys), с явным указанием полей, ограничений и связей с пользователями и группами. execution   ждёт
````

</details>

### 1.5. Разрешённый переход выполняется

```
$ python cli.py --память-в cli-check-memory --модель ds-flash --задача перенос-организаций --стадия execution
```

- ✓ стадия сменилась
- ✓ названы дальнейшие переходы
- ✓ код возврата 0

<details><summary>вывод</summary>

````
Задача «перенос-организаций», стадия planning (планирование).
Стадия: execution. Дальше можно: validation, planning.
````

</details>

### 2.1. Пауза по команде

```
$ python cli.py --память-в cli-check-memory --модель ds-flash --задача перенос-организаций --пауза
```

- ✓ сказано, что задача остановлена
- ✓ объяснено про текущий шаг
- ✓ подсказано, как продолжить
- ✓ код возврата 0

<details><summary>вывод</summary>

````
Задача «перенос-организаций», стадия execution (исполнение).
Задача «перенос-организаций» остановлена. этап исполнение; шаг Инвентаризация legacy-таблицы organization в схеме gissys: выгрузить DDL, список колонок, типы, ограничения, индексы, внешние ключи и объём строк из PostgreSQL 9.4. (1 из 12, план); на паузе; ждём: можно продолжать
Текущий шаг доведён до конца — ответ модели, за который уже заплачено,
выбрасывать незачем. Продолжить: --продолжить.
````

</details>

### 2.2. Пауза видна другому процессу

```
$ python cli.py --память-в cli-check-memory --модель ds-flash --задача перенос-организаций --состояние
```

- ✓ в карточке НА ПАУЗЕ
- ✓ названа причина «по команде»
- ✓ этап прежний — execution
- ✓ код возврата 0
- ℹ в файле задачи: пауза=True, причина=по команде, этап=execution

<details><summary>вывод</summary>

````
Задача «перенос-организаций», стадия execution (исполнение).
------------------------------------------------------------------------------
ЗАДАЧА «перенос-организаций» — перенос справочника организаций
------------------------------------------------------------------------------
  этап       execution (исполнение), НА ПАУЗЕ
             причина остановки: по команде
             дальше можно: validation, planning
  шаг        Инвентаризация legacy-таблицы organization в схеме gissys: выгрузить DDL, список колонок, типы, ограничения, индексы, внешние ключи и объём строк из PostgreSQL 9.4. (1 из 12, план)
  ожидание   продолжить — можно продолжать

  шаги:
     1. Инвентаризация legacy-таблицы organization в схеме gissys: выгрузить DDL, список колонок, типы, ограничения, индексы, внешние ключи и объём строк из PostgreSQL 9.4. execution   ждёт
     2. Собрать фактические данные о связях organization с остальными таблицами gissys (account, group, ms*, ol*) и с бизнес-таблицами gisdata, чтобы понять, где организация используется как фильтр видимости. execution   ждёт
     3. Уточнить у владельца системы, кто и как наполняет справочник организаций: вручную через интерфейс, из 1С или обоими путями, и есть ли у организации атрибуты, приходящие из 1С. execution   ждёт
````

</details>

### 2.3. Ручную задачу продолжать нечем

```
$ python cli.py --память-в cli-check-memory --модель ds-flash --задача перенос-организаций --продолжить
```

- ✓ объяснено, что задача не по сценарию

<details><summary>вывод</summary>

````
Задача «перенос-организаций», стадия execution (исполнение).
Ошибка: Не удалось продолжить задачу «перенос-организаций»: Задача «перенос-организаций» велась не по сценарию — продолжать нечего.
````

</details>

### 3.1. Сценарий по шагам встаёт на остановке

```
$ python cli.py --память-в cli-check-memory --модель ds-flash --по-шагам --сценарий напиши фичу Напиши фичу: слой колодцев на карте
```

- ✓ шаги печатаются по мере готовности
- ✓ прогон встал
- ✓ названо ожидание
- ✓ подсказано, как продолжить
- ✓ код возврата 0

<details><summary>вывод</summary>

````
[1/5] аналитик — planning, ds-flash…
------------------------------------------------------------------------------
# Требования к фиче «Слой колодцев на карте»

## Что должно получиться

1. На карте доступен отдельный слой «Колодцы», который можно включать и выключать независимо от остальных слоёв.
2. Колодцы отображаются точечными объектами с условным обозначением, принятым в системе для этого типа объектов.
3. Слой работает в обеих версиях интерфейса — полной (каждый слой отдельным слоем) и light (слои объединены в «Объекты ГРС»).
4. Слой доступен внешним потребителям через WMS/WFS наравне с остальными слоями сети.
5. Поведение слоя при масштабировании (видимость на разных зумах, генерализация) соответст…
[3 651 токенов (вход следующего шага)]

------------------------------------------------------------------------------
````

</details>

### 3.2. Задача сценария видна в списке

```
$ python cli.py --память-в cli-check-memory --модель ds-flash --задачи
```

- ✓ задача есть в списке
- ✓ помечена паузой
- ✓ код возврата 0

<details><summary>вывод</summary>

````
------------------------------------------------------------------------------
ЗАДАЧИ В РАБОЧЕЙ ПАМЯТИ — 2
------------------------------------------------------------------------------
  напиши-фичу-0920-0240      планирование на паузе  шаг 2/5      напиши фичу
                             ждём: шаг «аналитик» спрашивает: имя слоя колодцев в legacy mapfile и конфиге OpenLayers, его атрибутный состав с разделением «из 1С / из ГИС», правила стилизации и масштабные пороги, роль колодца в эмуляции отключений, допустимость редактирования из ГИС и объём слоя.
  перенос-организаций        исполнение   на паузе  шаг 1/12     перенос справочника организаций
                             ждём: можно продолжать
````

</details>

### 4.1. Продолжение в новом процессе (с ответом на вопрос шага)

```
$ python cli.py --память-в cli-check-memory --модель ds-flash --по-шагам --задача напиши-фичу-0920-0240 --ответ Колодцы лежат в gisdata отдельным слоем, SRID 3857.
```

- ✓ прогон пошёл дальше
- ✓ пройденное не переигрывается
- ✓ код возврата 0

<details><summary>вывод</summary>

````
Задача «напиши-фичу-0920-0240», стадия planning (планирование).

[2/5] архитектор — planning, ds-flash…
------------------------------------------------------------------------------
# Проектирование фичи «Слой колодцев на карте»

## 1. Модель данных

### 1.1. Основная модель `Well` (приложение `network`)

Соответствие legacy: таблица точечных объектов в схеме `gisdata`, которую читал MapServer через mapfile и редактировал `userpgplace.php` (`edit`, `savegeom`, `savenewcrash`, `starteditsession`).

Поля:

````

</details>

### 4.2. Продолжение в новом процессе

```
$ python cli.py --память-в cli-check-memory --модель ds-flash --по-шагам --задача напиши-фичу-0920-0240 --продолжить
```

- ✓ прогон пошёл дальше
- ✓ пройденное не переигрывается
- ✓ код возврата 0

<details><summary>вывод</summary>

````
Задача «напиши-фичу-0920-0240», стадия planning (планирование).

[3/5] backend — execution, ds-flash…
------------------------------------------------------------------------------
# Фича 0920-0240: слой колодцев — серверная часть

Ниже — только backend: модели, миграции, представления. Соответствие legacy-компонентам указано в комментариях к коду.

## 1. Структура приложения

```
network/
    __init__.py
    apps.py
````

</details>

### 4.3. Продолжение в новом процессе

```
$ python cli.py --память-в cli-check-memory --модель ds-flash --по-шагам --задача напиши-фичу-0920-0240 --продолжить
```

- ✓ прогон пошёл дальше
- ✓ пройденное не переигрывается
- ✓ код возврата 0

<details><summary>вывод</summary>

````
Задача «напиши-фичу-0920-0240», стадия execution (исполнение).

[5/5] ревьюер — validation, ds-flash…
------------------------------------------------------------------------------
# Ревью фичи 0920-0240 «Слой колодцев на карте»

Сверял три результата (архитектор, backend, фронтенд) между собой, с рамками проекта и с инвариантами. Ниже — расхождения, каждое с указанием, чем грозит.

## 1. Расхождения между шагами

1. **Фронтенд проектировался против несуществующего контракта.** В п. 1 фронтенда прямо сказано: «Проект архитектора обрезан, поэтому ниже — контракт, который я ожидаю от эндпоинтов». То есть фронтенд угадывал имена и форматы. Backend в свою очередь тоже обрезан, и в видимой части нет ни `urls.py`, ни `views.py`, ни `serializers.py` — только `models.py`. Сверить контракт не с чем.
   - Гроза: `GET /api/wells/?bbox=`, `POST /api/wells/{id}/geometry/`, `POST /api/editsession/start/` могут не совпасть с тем, что реально отдаёт backend. Это не «мелкая нестыковка», а разрыв контракта между слоями, который всплывёт на интеграции.

2. **Фронтенд сам признаёт, что это повод вернуть задачу на планирование.** Формулировка «это повод вернуть задачу на планирование, а не „догадаться“ в коде» — это не ревью-замечание, а признание, что стадия проектирования не завершена. На стадии validation это блокирующий дефект: проверять нечего, потому что контракт не зафиксирован.
````

</details>

### 5.1. Пути слоёв памяти

```
$ python cli.py --память-в cli-check-memory --модель ds-flash --файлы
```

- ✓ каталог рабочей памяти
- ✓ база диалога
- ✓ каталог пользователя
- ✓ код возврата 0

<details><summary>вывод</summary>

````
------------------------------------------------------------------------------
ГДЕ ЧТО ЛЕЖИТ
------------------------------------------------------------------------------
  краткосрочная (диалог)       cli-check-memory/dialog.db
  рабочая (задачи)             cli-check-memory/working
  долговременная (профиль)     cli-check-memory/long/инженер/profile.json
  долговременная (сценарии)    cli-check-memory/long/инженер/scenarios.json
  долговременная (решения)     cli-check-memory/long/инженер/decisions.jsonl
  долговременная (знания)      cli-check-memory/long/инженер/knowledge.json
  журнал маршрутизации         cli-check-memory/routing.jsonl
------------------------------------------------------------------------------
Три слоя — три разных хранилища, и это видно обычным ls.
````

</details>

### 6.1. Диалоговый режим

```
$ python cli.py --память-в cli-check-memory --модель ds-flash --задача перенос-организаций
```

- ✓ команда «состояние» отвечает
- ✓ «помощь» перечисляет команды
- ✓ выход без ошибок
- ✓ код возврата 0

<details><summary>вывод</summary>

````
Задача «перенос-организаций», стадия execution (исполнение).
------------------------------------------------------------------------------
Агент миграции ГИС. Пользователь «инженер», сессия «основная».
Слои: долговременная, краткосрочная, рабочая. Маршрутизатор: авто. Модель: ds-flash.
Профиль: без имени, на вы, подробно, код обязательно, списки.
Сценариев: 3 (запускаются по триггеру).
Задача «перенос-организаций», стадия execution (дальше можно: validation, planning).
«помощь» — список команд, «выход» — закончить.
------------------------------------------------------------------------------

Вы: ------------------------------------------------------------------------------
ЗАДАЧА «перенос-организаций» — перенос справочника организаций
------------------------------------------------------------------------------
  этап       execution (исполнение), НА ПАУЗЕ
````

</details>

## Состояние задачи сценария на диске

Задача `напиши-фичу-0920-0240` доведена до конца и свёрнута в журнал решений — в рабочей памяти её больше нет.

## Что это доказывает

| Требование дня | Шаги прогона |
|---|---|
| этап / шаг / ожидаемое действие | 1.2, 1.4б |
| задача не перепрыгивает этап | 1.3, 1.5 |
| пауза на любом этапе | 2.1, 2.2, 3.1 |
| продолжение без повторных объяснений | 4.x |
| состояние живёт вне процесса | 2.2, 3.2, 4.x |
| состояние читается глазами | 5.1 и файл задачи выше |

Каждый шаг — отдельный процесс `cli.py`. Между 3.1 и 4.x процесс,
запустивший сценарий, уже завершился: продолжение поднимает задачу
с диска и доделывает ровно оставшиеся шаги.
